# Configurations

This directory contains experiment configuration files for the project.

## Available configs

- `baseline.yaml`: default configuration for the CNN + BiLSTM forecasting pipeline.

Future experiments can be added here as separate YAML files.